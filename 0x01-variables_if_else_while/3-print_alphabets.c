#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (succession)
 */
int main(void)
{
	int l = 97;
	int p = 65;

	while (l <= 122)
	{
		putchar(l);
	}
	while (p <= 90)
	{
		putchar(p);
	}
	putchar('\n');
	return (0);
}
