#include <stdio.h>
#include "main.h"
/*
 * main -  program that prints its name, then '\0'
 *
 * @argc: input
 * @argv: string
 * Return: 0
 */
int main(int agrc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
